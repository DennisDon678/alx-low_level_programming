#include <stdio.h>

/**
 *main - Entry piont of the program
 *
 *Return: 0 on success
 */
int main(void)
{
    char n = 'a';

    for ( ; n <= 'z'; n++)
            putchar(n);
     putchar(10);
     return (0);
}
